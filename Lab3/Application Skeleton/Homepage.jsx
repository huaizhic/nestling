function Homepage() {
  // After user logs in, they will be directed to this main 'lobby'
  // contains articles tab, and about us information
  // also contains navigation bar at the top for user to redirect to other services
}

export default Homepage;
