import React from "react";
import "./DesiredProperty.css";

const Dropdown = ({ options, selectedOption, onSelect }) => {
  // individual dropdown component for re-use in multiple components
};

export default Dropdown;
