import "./ListingPanel.css";
import emptyimage from "../assets/images/emptyimage.png";
import { Link } from "react-router-dom";

export const ListingPanel = ({ title, price, percentageMatch }) => {
  // reusable component for displaying each property in current listing, mini panel style
  // displays picture, property name, price, address
};
